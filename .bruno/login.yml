info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/session/login
  body:
    type: json
    data: |-
      {
        "username": "dan",
        "password": "asdf"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
