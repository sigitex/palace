# Allow mutable slugs without redirects

Workspace and Board Slugs are editable URL locators rather than permanent identities. A Slug change immediately invalidates old links; Palace deliberately avoids alias history and redirects because they add permanent naming constraints and compatibility behavior that this greenfield tool does not need.
