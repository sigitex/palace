# Require a Workspace to be empty before deletion

Palace permanently deletes a Workspace only after its Boards have been removed explicitly. Cascading from Workspace deletion would let one confirmation erase a large content tree; requiring an Empty Workspace keeps irreversible Board deletion visible and deliberate.
